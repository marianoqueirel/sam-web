'use strict';

angular.module('payeSAM.controllers')
  .controller('ProviderCtrl', ['$rootScope', '$scope', '$location', '$window', 'Provider',
    function ($rootScope, $scope, $location, $window, Provider) {
      $scope.init = function () {
      };
  }]);
